# Level 4 – Questions I Asked

- Why can't a Kubernetes Service Account communicate with Google APIs?
- What does "Google API" actually mean in real scenarios?
- Why do pods need their own cloud identity separate from Terraform?
- Why isn’t Terraform’s impersonated identity inherited by pods?
- How does Workload Identity help in a multi-tenant platform?
- Why do banks forbid storing service account keys in containers?
- What does the metadata server do and why is it important?
- How do I verify which GSA my pod is actually using?
- What real benefit does WI give to an Infrastructure/Platform engineer?
